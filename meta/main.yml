galaxy_info:
  role_name: config_arch
  author: Daniel Lowry
  namespace: dandyrow
  description: Configures a fresh Arch install. Intended to carry out the tasks which are to be run in an arch-chroot environment after pacstrap is run.

  license: GPL-3.0-only

  min_ansible_version: "2.1"

  platforms:
    - name: ArchLinux
  
  galaxy_tags:
    - archlinux
    - linux
    - configuration
